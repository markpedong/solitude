'use client'

import React, { FC } from 'react'
import styles from './styles.module.scss'

const Products: FC = () => {
    return <div className={styles.productWrapper}></div>
}

export default Products
